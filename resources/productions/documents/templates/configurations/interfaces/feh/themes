# ---
# note:
#   - `conversion-timeout` is for filetype `svg`
#   - add `--start-at %F` in the `exec` section of `feh.desktop` entry
#     - to cycle through all the files in the directory
#     - not just the selected file, when `feh` is launched from a GUI file manager
# ---
feh --no-recursive \
    --edit \
    --scale-down \
    --draw-exif \
    --draw-filename \
    --draw-tinted \
    --image-bg black \
    --conversion-timeout 1
